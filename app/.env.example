# Copia este archivo a .env para que la app guarde en Supabase en vez de en el
# teléfono. Sin estas variables funciona igual, guardando en local.
#
# Salen del panel del proyecto, en Settings → API.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
